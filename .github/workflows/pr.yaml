name: pr-validation-workflow
on: 
   pull_request:
     types:
       - opened
       - synchronize
   push:
      branches:
        - develop
jobs:
  pr-validation-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome step
        run: echo "Welcome to Github Actions"
